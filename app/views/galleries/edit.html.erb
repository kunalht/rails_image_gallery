Edit Gallery form

<%= form_with(url: gallery_path(@gallery), model: @gallery, method: "patch") do |form| %>
  <%= form.text_field :name %>
  <%= form.text_area :description %>
  <br>
  <%= form.submit %>
<% end %>